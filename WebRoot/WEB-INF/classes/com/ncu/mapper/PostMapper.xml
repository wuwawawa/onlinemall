<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ncu.mapper.PostMapper">
    <!--实体类与数据库映射字段部分-->
    <resultMap id="ResultMapPost" type="com.ncu.po.Post">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="addTime" column="addTime" jdbcType="TIMESTAMP"/>
        <result property="isDelete" column="isDelete" jdbcType="INTEGER"/>
        <result property="clickAmount" column="click_amount" jdbcType="INTEGER"/>
    </resultMap>
    <!-- 声明数据库字段 -->
    <sql id="Post_field">
        id,user_id,title,content,addTime,isDelete,click_amount
    </sql>
    <!-- 实体类属性-->
    <sql id="Post_insert">
        #{id},#{userId},#{title},#{content},#{addTime},#{isDelete},#{clickAmount}
    </sql>
    <!-- 更新结果  -->
    <sql id="Post_update">
        <if test="userId != null">
            user_id = #{userId}
        </if>
        <if test="title != null">
            title = #{title},
        </if>
        <if test="content != null">
            content = #{content},
        </if>
        <if test="addTime != null">
            addTime = #{addTime}
        </if>
        <if test="isDelete != null">
            isDelete = #{isDelete}
        </if>
        <if test="clickAmount != null">
            click_amount = #{clickAmount}
        </if>
    </sql>
    <!-- 查询时条件   -->
    <sql id="Post_where">
        <if test="id != null">
            and id = #{id}
        </if>
        <if test="userId != null">
            and user_id = #{userId}
        </if>
        <if test="title != null">
            and title = #{title}
        </if>
        <if test="content != null">
            and content = #{content}
        </if>
        <if test="addTime != null">
            and addTime = #{addTime}
        </if>
        <if test="isDelete != null">
            and isDelete = #{isDelete}
        </if>
        <if test="clickAmount != null">
            and click_amount = #{clickAmount}
        </if>
    </sql>

    <!--    新增        -->
    <!--    参数：实体类-->
    <!--    返回：主键 -->
    <insert id="insert" parameterType="com.ncu.po.Post" useGeneratedKeys="true" keyProperty="id">
        insert into post(
        <include refid="Post_field"/>
        ) values(
        <include refid="Post_insert"/>
        )
    </insert>

    <!-- 根据实体主键删除一个实体-->
    <delete id="deleteById" parameterType="java.lang.Integer">
        delete from post where id=#{id}
    </delete>

    <!-- 通过实体删除-->
    <delete id="deleteByEntity" parameterType="com.ncu.po.Post">
        delete from post where 1=1
        <include refid="Post_where"/>
    </delete>

    <!-- 通过map删除 -->
    <delete id="deleteByMap" parameterType="java.util.HashMap">
        delete from post where 1=1
        <include refid="Post_where"/>
    </delete>

    <!-- 更新一个实体 -->
    <update id="update" parameterType="com.ncu.po.Post">
        update post
        <set>
            <include refid="Post_update"/>
        </set>
        where 1=1
        <include refid="Post_where"/>
    </update>

    <!-- 通过id进行修改-->
    <update id="updateById" parameterType="com.ncu.po.Post">
        update post
        <set>
            <include refid="Post_update"/>
        </set>
        where id=#{id}
    </update>

    <!-- 根据参数查询-->
    <select id="listByMap" resultMap="ResultMapPost" parameterType="map">
        select <include refid="Post_field"/>
        from post where 1=1
        <include refid="Post_where"/>
    </select>

    <!-- 查询整个表 -->
    <select id="listAll" resultMap="ResultMapPost">
        select <include refid="Post_field"/>
        from post
    </select>

    <!-- 查询所有实体,根据实体属性值为判断条件查询所有实体-->
    <select id="listAllByEntity" resultMap="ResultMapPost" parameterType="com.ncu.po.Post">
        select <include refid="Post_field"/>
        from post where 1=1
        <include refid="Post_where"/>
    </select>

    <!-- 根据主键获取一个实体-->
    <select id="load" resultMap="ResultMapPost" parameterType="java.lang.Integer">
        select <include refid="Post_field"/>
        from post where id=#{id}
    </select>

    <!-- 根据主键获取一个实体-->
    <select id="getById" resultMap="ResultMapPost" parameterType="java.lang.Integer">
        select <include refid="Post_field"/>
        from post where id=#{id}
    </select>

    <!-- 通过map查询-->
    <select id="getByMap" resultMap="ResultMapPost" parameterType="map">
        select <include refid="Post_field"/>
        from post where 1=1
        <include refid="Post_where"/>
    </select>

    <!--通过对象查询-不分页-->
    <select id="getByEntity" resultMap="ResultMapPost" parameterType="com.ncu.po.Post">
        select <include refid="Post_field"/>
        from post where 1=1
        <include refid="Post_where"/>
    </select>

    <!-- 通过map查询分页-->
    <select id="findByMap" resultMap="ResultMapPost" parameterType="map">
        select <include refid="Post_field"/>
        from post where 1=1
        <include refid="Post_where"/>
    </select>

    <!--通过对象查询分页-->
    <select id="findByEntity" resultMap="ResultMapPost" parameterType="com.ncu.po.Post">
        select <include refid="Post_field"/>
        from post where 1=1
        <include refid="Post_where"/>
    </select>

    <!-- 批量新增-->
    <select id="insertBatch" parameterType="java.util.List">
        insert into post(
        <include refid="Post_field"/>
        ) values
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.title},#{item.content},#{item.addTime})
        </foreach>
    </select>

    <!-- 批量修改-->
    <update id="updateBatch" parameterType="java.util.List">
        <foreach collection="list" item="item" index="index" separator=";">
            update post
            <set>
                <if test="item.title != null">
                    title = #{item.title},
                </if>
                <if test="item.content != null">
                    content = #{item.content},
                </if>
                <if test="item.addTime != null">
                    addTime = #{item.addTime}
                </if>
            </set>
            where 1=1
            <if test="item.id != null">
                and id = #{item.id}
            </if>
        </foreach>
    </update>

    <!-- 封装纯sql语法-->
    <!-- 查询一个对象返回map-->
    <select id="getBySqlReturnMap" resultType="map">
        ${sql}
    </select>

    <!-- 查询一个对象返回实体类-->
    <select id="getBySqlReturnEntity" resultMap="ResultMapPost">
        ${sql}
    </select>

    <!-- 查询一个对象返回map列表-->
    <select id="listBySqlReturnMap" resultType="map">
        ${sql}
    </select>

    <!-- 查询列表返回实体-->
    <select id="listBySqlReturnEntity" resultMap="ResultMapPost">
        ${sql}
    </select>

    <!-- 查询分页-->
    <select id="findBySqlRerturnEntity" resultMap="ResultMapPost">
        ${sql}
    </select>

    <!-- 通过sql修改-->
    <update id="updateBysql">
        ${sql}
    </update>
    <!-- 通过sql删除-->
    <delete id="deleteBySql">
        ${sql}
    </delete>
</mapper>